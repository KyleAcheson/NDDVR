This contains the sparse potential on a sobol grid, the sub-directory includes the results after fitting the  potential onto a denser grid.
You can use the fitted potential and grid in the subdirectory to run your own DVR calculations. 
The sobol grid training data is evaluated using DFT.
